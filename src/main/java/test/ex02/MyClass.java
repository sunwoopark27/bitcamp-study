package test.ex02;

public class MyClass {

  static int a = 100; 
  //클래스가 로딩된 후 자동으로 생성
  //클래스 이름으로 접근 가능해서
  // 클래스 필드라고도 한다(클래스 변수라고는 잘 안쓴다)

  int b = 200; //new 명령어 생성될 때!(그전에는 만들어지지 않음)
  //만들어지는 인스턴스 변수


}
